#include <iostream>
#include <fstream>
#include <sstream>

#include "UCTurma.h"

using namespace std;
/*
void readClasses(string filename){
    ifstream in(filename);
    string line;
    getline(in, line);
    while(getline(in, line)){
        istringstream iss(line);
        string a,b;
        getline(iss, a, ',');
        getline(iss, b, ',');
        UCTurma ucturma = UCTurma(a,b);

    }
}*/

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
